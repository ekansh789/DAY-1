## Ingress Deployment and Testing

* Deployed the complete application stack and performed ingress testing successfully.
* Created an Ingress YAML configuration with target type set to **Instance** mode for **HTTP**, and verified successful deployment.
* Created an Ingress YAML configuration with target type set to **IP** mode for **HTTP**, successfully verifying Pod IPs behind the ALB.
* Created an Ingress YAML configuration with target type set to **Instance** mode for **HTTPS** by creating and attaching an SSL certificate through ACM.
* Created an Ingress YAML configuration with target type set to **IP** mode for **HTTPS**.
* Added the required Route 53 DNS entry and verified that the application was accessible successfully.
